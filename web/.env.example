# Public Canvas BFF origin. Leave empty only when a same-origin reverse proxy serves /api.
VITE_CANVAS_BFF_URL=https://canvas-bff.example.test
# Exact Sub2API origin allowed to send the postMessage SSO handoff.
VITE_SUB2API_ORIGIN=https://sub2api.example.test
